name: Deployment

concurrency: deploy

on:
  push:
    branches:
      - develop

permissions:
  checks: write
  contents: write
  deployments: write

jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: Install dependencies
        run: npm ci

      - name: setup git config
        run: |
          git config --global user.name "Xinecraft"
          git config --global user.email "<kinnngg786@gmail.com>"

      - name: Deploy
        run: sh deploy.sh
